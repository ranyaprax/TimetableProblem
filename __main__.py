import GeneticAlgorithm as GA
import load_file as LF


# Load the problem instance from a file
# instance = LF.read_instance("instance.txt")

# Initialize the genetic algorithm with the loaded instance
# ga = GA.TPGeneticAlgorithm(
#     population_size=100,
#     mutation_rate=0.1,
#     crossover_rate=0.8,
#     generations=50,
#     selection_method="tournament"
# )

# Run the genetic algorithm to solve the timetable problem
# best_solution = ga.run_ga()

# Print the best solution found